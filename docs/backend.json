{
  "entities": {
    "user": {
      "title": "User",
      "description": "A user of the application.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The user's unique ID." },
        "email": { "type": "string", "format": "email", "description": "The user's email address." },
        "displayName": { "type": "string", "description": "The user's display name." },
        "photoURL": { "type": "string", "description": "The URL of the user's profile picture." },
        "role": { "type": "string", "enum": ["user", "operator", "shelterAdmin"], "description": "The user's role." }
      },
      "required": ["uid", "email", "role"]
    },
    "animal": {
      "title": "Animal",
      "description": "An animal available for adoption.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "species": { "type": "string", "enum": ["Cachorro", "Gato", "Coelho"] },
        "breed": { "type": "string" },
        "age": { "type": "number" },
        "size": { "type": "string", "enum": ["Pequeno", "Médio", "Grande"] },
        "gender": { "type": "string", "enum": ["Macho", "Fêmea"] },
        "description": { "type": "string" },
        "story": { "type": "string" },
        "personality": { "type": "array", "items": { "type": "string" } },
        "health": { "type": "array", "items": { "type": "string" } },
        "photos": { "type": "array", "items": { "type": "string", "description": "A photo of the animal, as a data URI." } },
        "shelterId": { "type": "string" },
        "createdBy": { "type": "string", "description": "The UID of the user who created the animal." }
      },
      "required": ["name", "species", "breed", "age", "size", "gender", "shelterId"]
    },
    "shelter": {
      "title": "Shelter",
      "description": "An animal shelter.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "address": { "type": "string" },
        "phone": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "website": { "type": "string", "format": "uri" }
      },
      "required": ["name", "address", "phone", "email", "website"]
    },
    "siteConfig": {
        "title": "Site Configuration",
        "description": "Global configuration for the site.",
        "type": "object",
        "properties": {
            "title": { "type": "string", "description": "The main title of the website." },
            "socialLinks": {
                "type": "array",
                "description": "A list of social media links.",
                "items": {
                    "type": "object",
                    "properties": {
                        "platform": { "type": "string", "enum": ["Instagram", "Twitter", "Facebook", "YouTube", "LinkedIn", "GitHub"] },
                        "url": { "type": "string", "format": "uri" }
                    },
                    "required": ["platform", "url"]
                }
            }
        }
    },
    "adoptionApplication": {
      "title": "Adoption Application",
      "description": "A request submitted by a user to adopt an animal.",
      "type": "object",
      "properties": {
        "animalId": { "type": "string" },
        "animalName": { "type": "string" },
        "animalPhoto": { "type": "string" },
        "shelterId": { "type": "string" },
        "shelterAdminId": { "type": "string" },
        "applicantId": { "type": "string" },
        "fullName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "address": { "type": "string" },
        "residenceType": { "type": "string", "enum": ["Casa", "Apartamento"] },
        "hasOtherPets": { "type": "string" },
        "reason": { "type": "string" },
        "agreement": { "type": "boolean" },
  "hcaptchaToken": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "accepted", "adopted"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
  "required": ["animalId", "shelterId", "shelterAdminId", "applicantId", "fullName", "email", "status", "hcaptchaToken"]
    }
  },
  "auth": {
    "providers": ["email", "google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/user" },
      "description": "Stores public information about a user."
    },
    "/animals/{animalId}": {
      "schema": { "$ref": "#/entities/animal" },
      "description": "Stores information about animals available for adoption."
    },
    "/shelters/{shelterId}": {
        "schema": { "$ref": "#/entities/shelter" },
        "description": "Stores information about partner shelters."
    },
    "/config/site": {
        "schema": { "$ref": "#/entities/siteConfig" },
        "description": "Stores global site configuration."
    },
    "/adoptionApplications/{applicationId}": {
      "schema": { "$ref": "#/entities/adoptionApplication" },
      "description": "Stores adoption requests submitted by users."
    }
  }
}
